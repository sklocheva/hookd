---
import Base from './Base.astro';

/**
 * Shared layout for the standing pages — about, licence, privacy, imprint.
 * One measure, one heading scale, so they read as a set rather than four
 * separately-styled one-offs.
 */
interface Props {
	title: string;
	description: string;
	heading: string;
	standfirst?: string;
	active?: 'patterns' | 'journal' | 'about';
	/** Shown under the heading, e.g. "Last updated 19 August 2026". */
	updated?: string;
}

const { title, description, heading, standfirst, active, updated } = Astro.props;
---

<Base title={title} description={description} active={active}>
	<article class="shell shell--narrow">
		<div class="head">
			<h1>{heading}</h1>
			{standfirst && <p class="standfirst">{standfirst}</p>}
			{updated && <p class="updated">{updated}</p>}
		</div>

		<div class="prose">
			<slot />
		</div>
	</article>
</Base>

<style>
	article {
		padding-bottom: 20px;
	}

	.head {
		padding-top: 44px;
	}

	h1 {
		margin: 0 0 18px;
		font: var(--type-page-title);
		color: var(--ink);
		text-wrap: pretty;
	}

	.standfirst {
		margin: 0 0 14px;
		max-width: var(--measure);
		font: var(--type-standfirst);
		color: var(--ink-2);
	}

	.updated {
		margin: 0;
		font: var(--type-label-sm);
		letter-spacing: 0.07em;
		text-transform: uppercase;
		color: var(--muted);
	}

	.prose {
		padding-top: 34px;
	}

	.prose :global(p),
	.prose :global(ul),
	.prose :global(ol) {
		max-width: var(--measure);
		margin: 0 0 20px;
		font: var(--type-body);
		color: var(--ink);
	}

	.prose :global(li) {
		margin-bottom: 10px;
	}

	.prose :global(h2) {
		max-width: var(--measure);
		margin: clamp(34px, 4vw, 46px) 0 16px;
		font: var(--type-article-h2);
		color: var(--ink);
	}

	.prose :global(h3) {
		max-width: var(--measure);
		margin: 32px 0 12px;
		font: var(--type-prose-label);
		letter-spacing: 0.06em;
		text-transform: uppercase;
		color: var(--ink);
	}

	.prose :global(strong) {
		font-weight: 600;
	}

	.prose :global(a) {
		color: var(--terracotta);
	}

	/* Marks a section that is deliberately unfinished and must be filled in before
	   the site is announced. Same convention as the design's visible placeholders. */
	.prose :global(.placeholder) {
		max-width: var(--measure);
		border: 1px dashed var(--border-dashed);
		padding: 18px 20px;
		margin: 0 0 20px;
		font: var(--type-callout);
		color: var(--muted);
	}

	.prose :global(.placeholder strong) {
		display: block;
		margin-bottom: 6px;
		font: var(--type-label-sm);
		letter-spacing: 0.07em;
		text-transform: uppercase;
		color: var(--ochre);
	}
</style>
